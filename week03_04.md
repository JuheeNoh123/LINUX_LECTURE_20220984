# 3, 4주차 - 계정 및 권한 관리

## 서버 운영 환경의 변화

### 1. 단일 서버 → 다중 컨테이너 확장
- 단일 서버에 app을 직접 관리
  - → 관리의 어려움, 장애 전파가 빠름

### 2. 독립된 Docker 컨테이너 배치
- 환경의 **일관성 보장**, **수평적 확장**

### Docker 컨테이너
- 주요 상위권 top 5 소프트웨어
  - Nginx, Apache, PostgreSQL, MYSQL, Redis, 인증, 백엔드 S/W 등
  - 주요 핵심 기능 : 웹서버 기반 처리
  - S/W 빌드, 소규모 컨테이너 관리에 적합

### Kubernetes
- 96%이상 기업 채택
- 공통 : 컨테이너를 개발, 배포 및 관리

---

## 컨테이너 기반 웹서버 관리 실습

### 1. Compose 확장 및 최적화

#### 주요 설정
- 이미지 태그 고정 : 버전 명시
- 컨테이너 이름 : my-web-proxy
- 재시작 정책 - 예기치 않은 문제 시 재부팅
- 네트워크 분리 - 사용자 정의 브릿지: 네트워크 공유 x

#### 특징
- 지난주에 단일 도커 실행시에는 명령어에 **직접 경로를 입력**해서 실행할때마다 타이핑을 했었다.

```bash
docker run -v ./html:/usr/share/nginx/html nginx
```

- 하지만, `volumes: ./html:/usr/share/nginx/html` 를 yml안에 적어두면,
  - → 매번 명령어에 -v 옵션 입력할 필요 없고, `docker compose up` 한번으로 자동 적용 된다.
  - → 또 **설정 파일에 고정**되어 있어서 실수할 일이 없다.

> **볼륨 매핑이란?**  
> 내 컴퓨터 폴더의 html 폴더와 컨테이너 안의 웹 루트 폴더를 연결(마운트)하는 것. 내 파일을 수정하면, 컨테이너에도 즉시 반영됨.

`docker-compose.yml` 설정 파일 예시:  
<img width="845" height="369" alt="image" src="https://github.com/user-attachments/assets/e2755168-332e-4735-b2c3-4213b2aea9ee" />

### 2. Compose 확장 설치 및 실행

**a. compose 설치**
```bash
sudo apt install docker-compose
```
- docker를 설치해도 compose는 자동으로 딸려오지 않아서 따로 설치 해줘야함

**b. 직접 실행하기**
```bash
docker-compose up -d
```
<img width="1753" height="80" alt="image" src="https://github.com/user-attachments/assets/36d1e3bc-5eb3-46dd-853d-9e048504197c" />

- 설정 파일 자동 로딩, 백그라운드 실행 (-d)

**c.** 기존 index.html 파일을 web_service/html 폴더로 복사한다.
<img width="852" height="194" alt="image" src="https://github.com/user-attachments/assets/852161ba-2f27-45ff-9366-03e79b51b945" />
<img width="865" height="197" alt="image" src="https://github.com/user-attachments/assets/e2a89d5b-2cc4-4fa7-9d78-341380d81b33" />

**d.** 그리고 로컬 웹서버를 확인해보면 잘 동작하고 있는걸 확인할 수 있다.
<img width="822" height="445" alt="image" src="https://github.com/user-attachments/assets/c07faf1a-8820-4c72-a829-cc552db30f10" />

### 3. Compose 제어하기

- 실행중인 compose 확인 / 정지
```bash
docker-compose ps
docker-compose stop  # 설정 파일이 있는 폴더에서 실행
```

| 명령어 | 컨테이너 삭제 | 네트워크 삭제 | 볼륨(데이터) 삭제 |
|--------|:---:|:---:|:---:|
| `docker-compose down` | O | O | X |
| `docker-compose down -v` | O | O | O |

> **볼륨 = 데이터가 저장되는 공간**  
> 컨테이너는 껐다 켜도 데이터는 유지되어야 하기 때문에, `down` 만 할 경우 데이터는 유지됨.  
> `-v` 옵션을 쓰면, **컨테이너+네트워크+저장된 데이터까지 싹 다 지움**  
> 호스트 매핑 폴더 (./html)은 내 컴퓨터 관할 폴더여서 **직접 삭제**해야한다.

---

## 다중 계정 생성 및 파일 권한 관리 실습

### 1. 폴더를 하나 생성하고 권한을 확인해보자

```bash
stat 폴더명
```
<img width="890" height="233" alt="image" src="https://github.com/user-attachments/assets/0501bd05-36a7-45cb-8b30-bc91c2d2d470" />

- 현재 소유자 : Uid, Gid 모두 njh19270

### 2. 현재 그룹 목록을 확인하고, 권한을 부여해보자

```bash
# 현재 그룹 목록 확인
getent group         # 전체
getent group www-data  # 특정, 기존

# 권한 부여하기
sudo chgrp www-data pds2_practice   # www-data의 담당 그룹을 바꿔라
sudo chmod 775 pds2_practice
```

> **www-data** : Nginx, Apache 같은 웹서버가 사용하는 그룹  
> - 설치할때 자동으로 만들어짐  
> - → 웹서버가 해킹 당해도 www-data 권한 밖의 파일은 건드리지 못하게 하기 위해 별도 계정으로 만듦

- 웹서버가 해당 폴더에 접근할 수 있도록 그룹을 바꿔주기
  <img width="894" height="209" alt="image" src="https://github.com/user-attachments/assets/385320d7-05b3-4c3b-b553-799dac9bce2b" />

  - → 웹 서버의 허용된 그룹에만 해당 권한 부여 (읽기, 쓰기, 실행)
- `755` : 소유자(7/rwx), 그룹(7/rwx), 그외 사람(5/r-x)
- 파일은 기본 644

### RBAC : 최소 권한 원칙 "이 사람은 이것만 할 수 있어"
모든 APP은 root 실행에 위험 존재 → 꼭 필요한 권한만 부여

---

## 계정 및 권한 확인하기

→ 계정을 생성하고 권한을 설정해보자

**1. 일반 계정 생성하기**
```bash
sudo adduser guest_practice
```
<img width="1219" height="580" alt="image" src="https://github.com/user-attachments/assets/989c69da-8635-4d72-be28-e4447531b64a" />

- home 폴더에 생성됨.

**2. 현재 pds2_practice 폴더로 이동 후 게스트 사용자의 접근 제한 확인하기**
```bash
sudo chmod 770 pds   # 게스트 사용자의 접근 제한
su guest
```
<img width="1081" height="207" alt="image" src="https://github.com/user-attachments/assets/178d6abe-a6ac-4748-ae70-e6c038eaa7f7" />

- 권한 제한 후 pds2_practice 파일에 접근 시도 시 Permission denied 된 것을 볼 수 있다.

**3. 계정 종류 확인해보기**
<img width="817" height="307" alt="image" src="https://github.com/user-attachments/assets/3b5cf712-fa0d-40ef-94d8-88f5968a9e94" />

```
계정 : 암호 : UID : GID : 설명 : 홈 : 로그인쉘
```

| UID 범위 | 계정 유형 |
|----------|----------|
| 0 | root |
| 1 ~ 999 | 시스템 / 서비스 계정 |
| 1000 이상 | 일반 사용자 |

**4. 계정 비밀번호 확인해보기**
<img width="879" height="136" alt="image" src="https://github.com/user-attachments/assets/88c711c1-cb85-432b-9eba-a64908157ef5" />

```bash
sudo cat /etc/shadow
```
- `계정 : 암호 : 최소 : 최대 : 경고 : 비활성 : 만료 : 예약`

---

## 특정 사용자에게 권한 주기 : nginx 재시작만 허용하기

guest_practice에게 nginx 재시작 할수 있는 권한을 허용해보자

**1. 권한 설정**
```bash
sudo visudo
```
<img width="875" height="172" alt="image" src="https://github.com/user-attachments/assets/10e11cdb-8c63-4b13-a0c8-733ca9939d43" />


**2. 테스트**
<img width="1442" height="120" alt="image" src="https://github.com/user-attachments/assets/fc99adb7-f2ae-49e8-aba2-b26c72aa27b2" />

- 명령어가 설정에서 설정한 명령어와 완전히 같아야한다.
- restart만 되고 stop은 안되는걸 볼 수 있다.
- 기존에 web_admin이 위와 같이 up에 대해서만 권한을 줬기 때문에 정지와 재시작이 안되고, `up -d` 명령어만 가능하다.

---

## 계정 생성 자동화

### /etc/skel
> 새 계정을 만들 때 자동으로 복사되는 템플릿 폴더

| 파일 | skel에서의 역할 |
|------|----------------|
| `.profile` | 신규 사용자 기본 로그인 환경 |
| `.bashrc` | 신규 사용자 기본 쉘 동작 |
| `.bash_logout` | 신규 사용자 기본 종료 동작 |

고려사항
- 로그인 소개 및 보안 경고문
- 로그인 쉘
- 패스워드 및 만료 설정
- 파일 접근 통제
- 히스토리 등

### 1. 파일 설정하기

```bash
# 동적 배너 수정
cd /etc/update-motd.d
sudo nano 00-header

# 배너 다시 실행
run-parts /etc/update-motd.d/
```
<img width="539" height="140" alt="image" src="https://github.com/user-attachments/assets/e7af3c2e-f270-438a-9f2a-d5be273d6c52" />
<img width="859" height="192" alt="image" src="https://github.com/user-attachments/assets/64479590-6b3c-4482-86e6-014a41dfc378" />

### 2. 사용자 환경변수 확인 `.profile`
<img width="743" height="645" alt="image" src="https://github.com/user-attachments/assets/d480056b-7a39-49f6-be7e-431b0e0346e6" />

**umask** : 새로 만들어지는 파일/폴더의 기본 권한을 결정하는 값

| 대상 | 기준 | umask 022 적용 | umask 077 적용 |
|------|------|---------------|---------------|
| 파일 | 666 | 644 (rw-r--r--) | 600 (rw-------) |
| 폴더 | 777 | 755 (rwxr-xr-x) | 700 (rwx------) |

- umask 022 → 다른 사람도 읽기 가능
- **umask 077 → 본인만 접근 가능 (더 보안 강화)**

### 3. 명령어 실행 내역 / 실행 시간 기록 확인 `.bashrc`

> **.bashrc 파일이란?**  
> bash 셸 시작 시 자동으로 실행되는 설정 파일  
> 히스토리, 프롬프트 색상, alias 등을 설정

```bash
# 명령어 실행 내역
cat .bashrc

# 명령어 실행 시간 기록
sudo nano .bashrc
```

<img width="751" height="434" alt="image" src="https://github.com/user-attachments/assets/3d92cf27-44ab-450e-9673-3a803cca24ab" />
<img width="710" height="110" alt="image" src="https://github.com/user-attachments/assets/1c4ff5df-4ef7-4494-92d4-3dd2fc269357" />

→ 이 설정을 하면 `history` 명령 실행 시 **각 명령어가 실행된 날짜/시간**이 함께 표시됨

| 쉘 종류 | 설명 |
|---------|------|
| sh | 최초의 유닉스 쉘, 기본 쉘의 원형 |
| bash | sh 확장 쉘 |
| csh | C언어 문법과 유사한 쉘 |
| tcsh | csh 기능을 확장한 쉘 |
| ksh | sh기반, 프로그래밍 기능 강화 |
| zsh | bash + tcsh 장점 결합 |

### 4. 다수 생성 자동화

```bash
sudo nano create_user.sh
```

```bash
#!/bin/bash
for i in {1..20}  # 반복문
do
    useradd -m -s /bin/bash "student$i"  # 홈, bash쉘 지정
    echo "student$i:password!" | chpasswd
    # 보안 설정 추가: 첫 로그인 시 비번 변경 강제
    chage -d 0 "student$i"
done
```

→ 사용자 20명 자동 생성

```bash
# 단순 환영 txt 파일 생성
sudo touch readme.txt

# 스크립트 권한 부여
sudo chmod +x create_user.sh
<img width="837" height="149" alt="image" src="https://github.com/user-attachments/assets/51f7c883-de04-4d54-81ea-35fb319a6b85" />

# 스크립트 직접 실행하기
sudo ./create_user.sh
```

→ 20개 계정이 생김 (`cd /home/`)
<img width="1032" height="86" alt="image" src="https://github.com/user-attachments/assets/0167289f-0872-4542-9d54-4a470eb1e3a5" />

#### 계정에 접근하기
<img width="787" height="404" alt="image" src="https://github.com/user-attachments/assets/96ab460f-7655-4bc4-ace6-84fed8377df1" />

- 루트 계정으로 student9 파일에 접근하고자 하면 권한 없음 에러가 난다.
- 직접 student9로 로그인을 한다면, 아까 설정했던 리드미 등 skel 설정한 내용이 복사된걸 확인할 수 있다.

```bash
# 계정의 패스워드 만료 확인
sudo chage -l student9
```

---

## 팀 공유 폴더와 SetGID

### 학생 1~5로 이루어진 개발팀 생성 (dev_team1)

```bash
# 1. 기존 웹관리자 정보 확인
id web_admin  

# 2. 그룹 추가
sudo groupadd dev_team1

# 3. 팀의 그룹 번호 확인
grep "dev_team1" /etc/group

# 4. 학생을 모두 그룹에 추가
sudo nano team_user.sh
```
<img width="738" height="48" alt="image" src="https://github.com/user-attachments/assets/cd723df7-40da-49a8-b278-dc187ad8168e" />
<img width="476" height="201" alt="image" src="https://github.com/user-attachments/assets/0f28c84d-47c8-4fe8-ab01-4c366b17638f" />
<img width="649" height="41" alt="image" src="https://github.com/user-attachments/assets/5c8c7083-532d-41f8-959a-750bd41af48e" />


### 특정 공유 폴더 권한 부여

**1. 공유 폴더 만들기**
```bash
# 1. 팀장(학생1) 계정으로 전환
su student1

# 2. 학생1 폴더이동
cd student1

# 3. 공유폴더생성
mkdir dev_share
stat dev_share  # 기본권한 : 775 (외부 접근 O)
```

**2. 그룹 권한 설정**

**a. 그룹 권한 설정**
```bash
chgrp dev_team1 dev_share
```
- dev_share 폴더의 그룹 소유권을 dev_team1으로 변경

**b. setgid 설정 (`chmod 2750`)**
```bash
# student1은 sudo 권한 없으므로 내 계정에서 실행
sudo chmod 2750 /home/student1/dev_share
```

> **setgid의 2의 의미 :**  
> 이 폴더 안에서 새파일을 만들면 → 자동으로 **dev_team1 그룹 권한이 부여**됨  
> 일반적으로는 파일 만든 사람의 기본 그룹이 붙지만, SetGID가 있으면 **폴더의 그룹을 상속**해줌

**c. `stat dev_share`**
<img width="733" height="180" alt="image" src="https://github.com/user-attachments/assets/e8f3b8af-ac18-4174-b98a-e623542d2ee0" />


**3. 그룹 권한 확인**
<img width="665" height="224" alt="image" src="https://github.com/user-attachments/assets/8484afd5-8846-4c34-a8f9-a2c8f62ef2f1" />

SetGID는 소문자 `s`로 표시됨.

```
drwxr-s---
```
- `d` : 디렉토리
- `rwx` : 소유자 (student1)
- `r-s` : 그룹 (dev_team1) ← SetGID 적용

| | SetUID (권한값: 4) | SetGID (권한값: 2) |
|--|--|--|
| 의미 | 이 프로그램을 실행하는 동안, **파일 소유자의 권한**으로 실행 | 이 프로그램을 실행하는 동안, **파일 소유 그룹의 권한**으로 실행 |
| 예시 | `passwd` 명령어 : 일반 유저가 실행해도 root 권한으로 `/etc/shadow` 수정 가능 | 공유 폴더에 파일 생성시 자동으로 해당 그룹 권한 부여 |

| 표시 | 의미 |
|------|------|
| `s` (소문자) | SetUID/GID + 실행권한 **있음** |
| `S` (대문자) | SetUID/GID는 있지만 실행권한 **없음** |

**4. 임시파일 생성 후 학생 접근 확인**
```bash
touch report1.hwp
```
<img width="778" height="185" alt="image" src="https://github.com/user-attachments/assets/7b59930f-bb6d-4bd7-91da-b22a8621cdba" />

→ 그룹에 없는 student9는 권한이 없는 것을 알 수있다.

---

## 세부 권한 조정 (ACL)

### 필요한 이유
기존 리눅스 권한 (chmod)은 **그룹 전체**에만 권한 설정이 가능함. 특정 팀원 한명에게만 다른 권한을 주려면, **ACL이 필요!**

### 실습 시나리오 목표

| 사용자 | 권한 |
|--------|------|
| student1 (팀장) | 모두 가능 |
| student2 | 목록 x, 실행 o |
| student3~5 | 목록 o, 실행 x |
| student6 (외부인) | 모두 차단 |

### 1. test.sh 파일 생성 (student1)

```bash
nano test.sh
chmod 700 test.sh  # 소유자만 실행, 그룹 권한 없앰
```
<img width="623" height="125" alt="image" src="https://github.com/user-attachments/assets/b70d38dc-f60b-4342-be32-fbfe0a05fe82" />

### 2. ACL 설치
```bash
sudo apt-get install acl
```

### 3. student2에게 실행 권한 부여

```bash
# 파일에 실행 권한
setfacl -m u:student2:r-x test.sh  # r이 있어야 .sh 실행 가능!

# 폴더에도 진입 권한 필요
setfacl -m u:student2:--x dev_share
```
<img width="741" height="297" alt="image" src="https://github.com/user-attachments/assets/29dadcd0-24e9-4885-bed2-b3b9eb1979bf" />

### 4. ACL 확인
```bash
getfacl test.sh
getfacl dev_share
```
<img width="515" height="437" alt="image" src="https://github.com/user-attachments/assets/01ebcc24-2a18-451f-aee2-32115862e5c3" />

### 5. 결과확인
<img width="580" height="101" alt="image" src="https://github.com/user-attachments/assets/83103d32-d2d4-4a24-a60a-ab9bcd1c01b1" />
<img width="579" height="128" alt="image" src="https://github.com/user-attachments/assets/fd30caf4-2dbf-41bf-af0f-24af34f7a9e4" />

- 같은 그룹 내의 3번 학생은 권한 없으나, 2번 학생은 실행 성공

| 권한 조합 | 결과 |
|-----------|------|
| R만 있음 | 파일 목록은 보이나 접근 불가 |
| X만 있음 | 목록은 안 보이나 파일 접근 가능 |
| R + X | 목록 확인 + 접근 가능 |
| X 없음 | 디렉토리 진입 불가 |

### setfacl 명령어 구조

```
setfacl [옵션] [대상] [파일]
  -m : 권한 부여
  -b : 파일 권한 삭제
  -x : 사용자 권한 삭제
  u:A : A 유저에게
  g:B : B 그룹에게
  r/w/x : 읽기/쓰기/실행
```

---

## 실습 문제

### 1. 권한 변경하기
- docker를 web_admin 권한으로 실행 : 재시작과 정지는 제한

<img width="675" height="161" alt="image" src="https://github.com/user-attachments/assets/0e16c5c3-366a-4f28-8ca6-2facfe2d0402" />

- 기존에 web_admin이 `up -d`에 대해서만 권한을 줬기 때문에 정지와 재시작이 안되고, `up -d` 명령어만 가능하다.  
<img width="1162" height="166" alt="image" src="https://github.com/user-attachments/assets/85017b56-fd60-44ba-89ce-07d2ec71c34a" />

### 2. 모든 계정 삭제하기

스크립트를 생성하여 6~20번 계정 삭제

<img width="637" height="921" alt="image" src="https://github.com/user-attachments/assets/b8fd7c5f-1d08-49f1-9437-eb4b71257e99" />


> `userdel -r` 옵션이 홈디렉토리 + 메일함을 같이 삭제하는 옵션인데, 이 학생들은 메일을 한번도 받은적이 없어서 메일함 자체가 생성된적 없는것이었다. **(경고 - 오류아님)**  
> 리눅스는 기본적으로 시스템 내부 메일 기능이 있다. `/var/mail` 폴더에 각 사용자별 메일함이 생기고, 인터넷 이메일이 아니라 시스템 내부에서 root가 사용자한테 보내는 알림 메시지 같은 용도이다.

### 3. 웹 서비스 폴더

`/html` 폴더 내에 팀 공유 설정 (학생 1~5를 그룹에 추가, 3,4번은 팀원 탈퇴함 → 접근 제한)

**a.** 우선 html 폴더를 만들어준다 (student1)
```bash
mkdir html
```
<img width="633" height="281" alt="image" src="https://github.com/user-attachments/assets/1cd3efe3-024e-4814-a367-9eca8ef0e001" />

**b.** html의 폴더의 그룹 소유권을 dev_team1으로 변경해준다
```bash
chgrp dev_team1 html
```
<img width="441" height="30" alt="image" src="https://github.com/user-attachments/assets/8c97a2c9-43d0-4ad3-85d6-da0e5cb6356a" />

**c.** 내 계정에서 해당 폴더의 권한을 2750으로 제한해준다 (외부 접근 X, 그룹 r-s 권한 설정)
```bash
sudo chmod 2750 /home/student1/html
```
<img width="737" height="343" alt="image" src="https://github.com/user-attachments/assets/022eb445-536f-445a-b876-1a221b295b5f" />

**d.** 3, 4번 학생은 세부 설정으로 권한을 제한해준다
```bash
setfacl -m u:student3:--- html
setfacl -m u:student4:--- html
getfacl html
```
<img width="530" height="266" alt="image" src="https://github.com/user-attachments/assets/0f483336-89d1-46ab-8354-da4c38d78211" />

**e. 결과 확인**
<img width="523" height="255" alt="image" src="https://github.com/user-attachments/assets/ba1673db-e5e9-4b94-b2c2-54b285c7a584" />

- html 폴더 안에 아무것도 없긴 하지만 student3, 4번은 탈퇴했기 때문에 `ls` 명령어 자체가 제한된다.
- student2는 잘됨.
